# Explore JS with vue devtools

In vue devtool, in the "Components" tab, when a component is selected, a little `= $vm0` appears. This variable can then be used in the navigator console to explore the virtual DOM of this component.
